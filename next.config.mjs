/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "c1.wallpaperflare.com",
      "openweathermap.org",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
