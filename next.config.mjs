/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "c1.wallpaperflare.com",
      "openweathermap.org",
      "images.unsplash.com",
      "apod.nasa.gov",
    ],
  },
};

export default nextConfig;
